server:
  port: 8800

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always
    gateway:
      enabled: true

spring:
  cloud:
    gateway:
      routes:
        - id: nginx-service
          uri: http://nginx.edu25.svc
          predicates:
            - Path=/**
          filters:
            - RewritePath=/(?<path>.*),/$\{path}
        - id: httpd-service
          uri: http://httpd.edu25.svc
          predicates:
            - Path=/apache/**
          filters:
            #- RewritePath=/apache/(?<segment>.*)","/${segment}
            - RewritePath=/apache/(?<path>.*),/$\{path}

logging:
  level:
    org.springframework.cloud.gateway: info
    reactor.netty: info
    com.kt: trace
